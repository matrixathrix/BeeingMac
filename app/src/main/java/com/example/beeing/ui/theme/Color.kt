package com.example.beeing.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Hive identity — streak/ring accents, not score bands (see scoreBandColor).
val HoneyGold = Color(0xFFFFB300)
val RingQualifiedGreen = Color(0xFF66BB6A)

// Fixed accent palette matching the reference check-in screenshot — used
// deliberately in place of Material You's dynamic primary for the tag
// picker and Save CTA, so those specific elements always read as this warm
// orange regardless of wallpaper. Everything else in the app keeps dynamic
// color untouched.
val AccentOrange = Color(0xFFF2994A)
val ChipNeutralFill = Color(0xFFEDE3D5)
val ChipNeutralText = Color(0xFF8A7F72)